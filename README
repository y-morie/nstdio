N-STDIO Library
==============

0. Overview
1. Installation

0. Overview
-----------



1. Installation
---------------
The simplest way to install N-STDIO library is typically a combination of 
running "configure" and "make".  Execute the following commands to install 
the N-STDIO library system from within the directory at the top of the tree:

shell$ ./configure --prefix=/where/to/install
[...lots of output...]
shell$ make all install

See INSTALL for more details.
